package threading;

public class UsingYield {

	/*
	 * Yield is a hint to the scheduler that the thread is willing
	 * to yield its current use of a processor though this may have
	 * no effect
	 */
	
}
