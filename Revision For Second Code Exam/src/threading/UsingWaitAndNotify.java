package threading;

public class UsingWaitAndNotify {

	/*
	 * wait() puts a thread in a waiting pool where it waits for another
	 * notify() or notifyAll()
	 * notifyAll() wakes up all the threads waiting on this object
	 * notify() wakes only one thread chosen by the scheduler
	 */
}
