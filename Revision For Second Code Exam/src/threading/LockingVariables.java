package threading;

public class LockingVariables {

	/*
	 * Threads do not release their lock when they go to sleep
	 * Each object has one lock
	 * If a thread has an objects lock then it is the only thread that
	 * can execute any of the objects critical sections
	 */
	public static void main(String[] args){
		
	}
	
	/*
	 * if methods f1(), f2() and f3() are synchronized, then they cannot
	 * be executed by any other thread
	 */
}
